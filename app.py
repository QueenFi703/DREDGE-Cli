# DEPRECATED - Root app.py is not used by Vercel
# Entry point is now: api/gateway.py:app
# 
# This file is kept for reference only.
# To use this file locally, run:
#   python -m uvicorn api.gateway:app --host 0.0.0.0 --port 9000
#
# Or remove this file if not needed for local development

# Original content moved to app.py.backup
# See pyproject.toml [tool.vercel] section for Vercel configuration
