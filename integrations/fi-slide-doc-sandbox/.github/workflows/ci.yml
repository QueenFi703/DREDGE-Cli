name: Fi Slide & Document Sandbox CI

on:
  push:
    paths:
      - 'integrations/fi-slide-doc-sandbox/**'
  pull_request:
    paths:
      - 'integrations/fi-slide-doc-sandbox/**'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: integrations/fi-slide-doc-sandbox
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: integrations/fi-slide-doc-sandbox/package.json
      - name: Install dependencies
        run: npm install --ignore-scripts
      - name: Syntax check
        run: npm run check
      - name: Smoke test server
        run: |
          node server/index.mjs > /tmp/fi-sandbox.log 2>&1 &
          PID=$!
          trap 'kill $PID' EXIT
          for i in {1..20}; do
            if curl -fsS http://127.0.0.1:8787/api/health; then exit 0; fi
            sleep 1
          done
          cat /tmp/fi-sandbox.log
          exit 1

  thresh:
    runs-on: ubuntu-latest
    needs: test
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Thresh workflow analysis
        uses: QueenFi703/QueenFi703-thresh-agent@5f047a931b266eda5648851b3aa0f507b0b048a3
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          commit-fixes: 'false'
          pr-number: ${{ github.event.pull_request.number }}
