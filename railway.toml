[build]
dockerfile = Dockerfile.prod

[env]
PYTHONUNBUFFERED = true
FLASK_ENV = production

[deploy]
startCommand = "uvicorn unified_auth_gateway:app --host 0.0.0.0 --port $PORT"
healthcheckPath = /health
healthcheckTimeout = 100
